# Deploy Firestore rules (no `firebase` CLI needed)

1. Open https://console.firebase.google.com → project **haibo-tours**
2. **Firestore Database** → **Rules** tab
3. Copy everything from `firebase/firestore.rules` in this repo
4. Click **Publish**

Without this, admin login may work but **saving** content in the CMS will fail.
